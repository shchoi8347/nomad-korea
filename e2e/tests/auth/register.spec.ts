/**
 * 인증 — 회원가입 E2E 테스트
 *
 * 커버 시나리오:
 *  - 유효한 정보로 회원가입 → 성공 처리
 *  - 이미 존재하는 이메일 → 에러 메시지 표시
 *  - 비밀번호 불일치 → 유효성 검사 에러
 *  - 이메일 형식 오류 → 유효성 검사 에러
 *  - 비밀번호 최소 길이 미충족 → 유효성 검사 에러
 */
import { test } from '@playwright/test'

test.describe('회원가입', () => {
  test.skip('유효한 정보로 회원가입 시 성공 처리된다', async () => {})
  test.skip('이미 사용 중인 이메일로 가입 시 에러 메시지가 표시된다', async () => {})
  test.skip('비밀번호와 확인 비밀번호 불일치 시 유효성 검사 에러가 표시된다', async () => {})
  test.skip('이메일 형식이 잘못된 경우 유효성 검사 에러가 표시된다', async () => {})
  test.skip('비밀번호가 최소 길이 미충족 시 유효성 검사 에러가 표시된다', async () => {})
})
