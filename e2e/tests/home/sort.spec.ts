/**
 * 홈 페이지 — 정렬 기능 E2E 테스트
 *
 * 커버 시나리오:
 *  - 기본 정렬(좋아요 많은 순) → 내림차순으로 도시 표시
 *  - 좋아요 적은 순 → 오름차순으로 도시 표시
 *  - 이름 오름차순 → 가나다 오름차순으로 도시 표시
 *  - 이름 내림차순 → 가나다 내림차순으로 도시 표시
 *  - 정렬 변경 후 필터와 함께 적용 → 정렬 + 필터 동시 동작
 */
import { test } from '@playwright/test'

test.describe('홈 정렬', () => {
  test.skip('기본 정렬은 좋아요 내림차순이다', async () => {})
  test.skip('"좋아요 적은 순" 선택 시 오름차순으로 도시가 정렬된다', async () => {})
  test.skip('"이름 오름차순" 선택 시 가나다 오름차순으로 정렬된다', async () => {})
  test.skip('"이름 내림차순" 선택 시 가나다 내림차순으로 정렬된다', async () => {})
  test.skip('정렬 변경 후 필터를 적용해도 정렬이 유지된다', async () => {})
})
